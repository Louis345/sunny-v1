# Bugs

- 2026-09-12 human-caught companion proof gap: the final spelling board contained the companion layer and a valid VRM URL, but screenshot capture occurred before the asynchronous model load completed, so Elli appeared missing. The human caught the absent character visually; logs and the browser lab proved only that the React container existed, not that the VRM loaded and rendered. Companion surfaces now expose a factual `idle/loading/ready/error` model state. The real browser proof waits for `ready` and a nonzero visible WebGL canvas before capture at both supported viewports; it does not use a fixed delay or force Elli to speak. A failed model load is now observable instead of becoming an unexplained empty layer.

- 2026-09-12 human-caught spelling adaptation identity gap: the targeted Planner received valid observation IDs and per-construct counts, but spelling constructs were opaque hashes with no factual word-to-outcome map. A human asking whether six known and four missed words would materially change the board exposed that the model could know the totals without reliably knowing which four words needed practice. Logs proved evidence persistence and citation validity but not semantic joinability; prior labs asserted IDs and final coverage separately. The Discovery summary now supplies each frozen assignment word with its item/construct IDs, independent-correct/incorrect counts, assistance, ambiguity, and observation IDs. The targeted-cycle boundary requires every clean independent miss to receive practice, requires at least one focused practice cohort when secure words also exist, and still requires the final hidden checkpoint to cover every assigned word. The Planner retains authority over activities, order, and presentation; no score threshold or learning-style label was added. A ten-word regression proves six secure words avoid equal practice, four misses receive practice, and all ten return in the final check.

- 2026-09-12 human-caught spelling board-density regression: the live targeted board hid Planner-authored activities beyond two routes/two nodes, projected a shared final checkpoint into an occupied route slot, replaced distinct Planner labels with repeated generic prefixes, and displayed the Word Radar thumbnail for unrelated native games. The human saw a cluttered, incoherent map despite `board_ready` logs; logs counted readiness but did not compare Planner node identities, slots, labels, or activity artwork with the published board. Earlier AI layout labs used smaller plans and a 2048-pixel synthetic renderer, so they never exercised the real dense board at Sunny's supported frames. The shared board projection now preserves every planned node, supports three nodes per route, converges routes through the final checkpoint, preserves authored labels with compact distinguishing suffixes, and resolves thumbnails from one shared activity registry. Publication rejects missing plan nodes, duplicate slots, graph/choice errors, and static activity-thumbnail mismatches. The saved school-sheet replay reused all provider receipts, passed the publication gate with ten visible nodes, and passed live 1280x720 and 1365x768 screenshots without modifying family data.

- 2026-09-09 human-caught diagnostic-authority gap: spelling intake captured words, then code always selected Word Radar. The human asked why the Planner was not choosing the evaluation; logs showed successful capture/publication but no selection decision, and labs asserted that the fixed game launched rather than that an academic decision existed. Fresh intake now asks the existing Planner to choose from explicitly validated diagnostic capabilities or record `needs_instrument`, with cited evidence, rationale, uncertainty and next evidence needed. Code freezes and projects that choice; legacy saved requests are explicitly logged as fixed-instrument compatibility. The native Word Radar assessment is the only validated option today; ordinary voice/timed recall and other practice games are not promoted. Tests cover missing/invalid selections, unavailable instruments, no fallback, immutable selection/config, downstream context, and one-call menu/restart behavior. Raw intake responses are durably received before validation. The menu lab now includes actual instrument source so eligibility is exercised, not implicitly bypassed by a partial fixture. No paid calls or family changes. Homophone stimulus validity remains a declared limitation, not a repaired learning claim.

- 2026-09-09 human screenshot: summoning Elli during Word Radar revealed a large host portrait overlapping a second collapsed portrait and obscuring the dismissal hint. App and Word Radar both rendered CompanionLayer; collapsed portraits overlapped exactly, concealing the duplicate until a human summoned Elli. Logs recorded valid companion presence but no UI ownership violation, and the lab tested Word Radar without companion props and the host overlay separately. A red launch-binding invariant now covers both actual App Word Radar call sites. Removing their companion prop leaves the one shared, care-aware host in control; standalone Word Radar demos retain their optional portrait. This is a two-line deletion, not a generated-content repair or change to learning behavior. Fresh visual replay of the corrected summoned state remains required.

- 2026-09-09 independent capability review: a spelling node could declare an available `activityId` but select another, unavailable `type`; normalization prioritizes the latter and validation previously checked only the former. This could make a child launch the wrong game even though the recorded Planner choice appeared valid. Earlier labs supplied equal identity/type fields; logs recorded the declared activity, not proof of the renderer binding. Three red tests reproduced both the overly broad schema and the runtime mismatch. The new Discovery handoff derives both fields and their allowed pairs from the launchable catalog and validates the same binding after normalization. Registered renderer aliases remain allowed; activity count, mechanics and choice remain Planner-owned.

- 2026-09-09 live spelling stimulus limitation: the hidden-word evaluation speaks isolated words without a contextual sentence. For assigned homophones such as `know` and `write`, successful audio playback alone does not prove that the intended spelling was distinguishable from `no` or `right`. The developer replay selected Not sure rather than claiming this ambiguity demonstrated a child gap. Prior fixtures used unambiguous words, and audio-success logs only establish delivery, not semantic disambiguation. The scoring boundary now classifies an alternate valid homophone spelling as `instrument_ambiguous` and excludes it from independent evidence; a common misspelling such as `nite` remains incorrect rather than becoming ambiguous. A future contextual sentence may improve the child experience, but it must preserve the hidden spelling and assistance contract rather than reveal letters or hardcode preferences.

- 2026-09-09 spelling multi-node title loss: the new real-parser/transport fixture supplied distinct Planner titles (`Word workshop`, `Recall check`), but the compact node schema stripped them. Canonical conversion gave both nodes `Learning Activity`, so publication rejected `learning_cycle_duplicate_child_title`. Existing labs injected already-hydrated plans and preserved titles without exercising the parser. Three tests failed red: normal multi-node publication, absent tool-schema title, and a missing title accepted locally. The parser now retains authored titles, targeted spelling's existing tool schema requires them, and local validation rejects missing titles with the node ID. Legacy/non-Discovery titles remain optional; code invents no replacements. The multi-node worker regression also resumes from the saved raw response without another provider call or changed observations/predictions.

- 2026-09-09 empty preparation failure: the actual spelling journey reached a Planner failure before any node was ready, but its attention panel promised that ready activities were still playable. A human returning from coffee would look for nonexistent choices. The worker log correctly reported failure; previous UI tests covered a failed node beside a ready sibling, not an empty or entirely evidence-locked board. Two red component regressions now require parent-help wording without a playable-sibling promise unless the generation status actually contains a ready node. The change replaces only status copy selection, not readiness, progression, or evidence behavior.

- 2026-09-09 targeted Planner response durability: the live post-Discovery validation rejection occurred after Anthropic returned, but the worker's outer receipt included parsing/hydration/validation and left the paid request in-flight without its response. Logs conflated a deterministic local rejection with unknown transport outcome and omitted the rejected node/activity; higher-level lab Planner mocks skipped this boundary. A transport-mocked, physically isolated regression reproduced the loss. The existing receipt stage now checkpoints the entire Anthropic message and original timing before local parsing, preserves completed legacy receipts, and refuses silent in-flight retries. Local validation reports node/activity details; raw replay retains usage, plan timestamps, observations, and predictions. The already-lost live response remains unrecoverable and must not be silently retried. The real-parser fixture also exposed an independent existing multi-node title loss (`learning_cycle_duplicate_child_title`); that is reported separately, not fixed or accepted by this durability increment.

- 2026-09-09 live spelling post-Discovery Planner rejection: the real 20-word school-sheet run completed and queued one worker, but local validation rejected `unknown_activity_id`. The response schema offered all 29 global activity identities while validation accepted only the packet's launchable catalog. The schema now uses that same available-capability set for spelling Discovery handoff, preserving every actually launchable choice and non-Discovery contracts. Three red tests reproduced the mismatch. The log omitted the rejected node identity, and the outer paid-stage wrapper did not save the response before hydration/validation threw; therefore this particular paid response is not recoverable and its exact rejected identity is unknown. Raw-response checkpointing is under correction; do not silently retry the existing in-flight receipt. Prior recorded tests supplied valid IDs and never tested an actual invalid received reply across the receipt boundary. All 20 canonical observations remain intact in the isolated certification; no targeted board was published.

- 2026-09-09 spelling intake summary contradicted published Discovery: the human's live CLI reached a working native Word Radar evaluation, then reported `Done — BLOCKED` and zero activities. The final summary still checked generated two-arm teaching/artwork requirements; its own logs contradicted the successful publication log. Prior AI labs asserted process exit and canonical publication, not the caregiver-facing summary/report. A real isolated menu regression reproduced the contradiction on initial intake and saved-response reuse. The summary now reports `DISCOVERY_READY` only for a matching published canonical evaluation, frozen native config, and launchable hidden-response Word Radar; it explicitly says the targeted board awaits Discovery evidence. Missing/mismatched publication, config, identity, or launch stays blocked. Summary reads preserve observations and make no provider calls; targeted-board readiness gates are unchanged.

- 2026-09-09 calibration-boundary follow-through: the human's review question exposed that direct callers of the exported repository could still persist an unsupported supported/falsified claim even after closure was blocked. Known returned-work callers were guarded, so their green tests and blocked-closure checks did not prove the lower-level claim boundary. Two direct-call regressions failed red; the repository now rejects those claims before theory mutation or persistence when the current lifecycle is awaiting calibration. Three additional cases preserve non-calibration evaluating decisions, and the existing uncertain-decision cases remain valid. No new threshold, provider call, or historical-data rewrite.

- 2026-09-09 insufficient calibration closure: the human-requested independent math/shared review reproduced an unknown-eligibility prediction labeled `insufficient` whose recorded `supported` decision nevertheless closed `awaiting_calibration`. The evidence label was correct, but downstream citation-presence checks ignored sufficiency and observation/source linkage; prior AI labs tested the label and successful closure separately, so green tests and lifecycle logs did not prove this boundary. One shared check now requires every cited calibration evaluation to be sufficient and linked to a cited external observation from its own source. Unsupported supported/falsified conclusions are rejected without changing facts; revised/inconclusive/awaiting-calibration decisions can preserve uncertainty without closing. Thirteen isolated regressions cover the exact reproduction, disconnected/mixed citations, valid graded/delayed closure, immutable evidence, and one-call interpretation. The math catalog comparison also now supplies a temporary lab profile instead of depending on real Reina data. No pedagogy thresholds, paid calls, or family-data changes.

- 2026-09-09 fresh-browser microphone dismissal: declining/dismissing optional microphone permission on a homework session was marked as a fatal voice error, clearing the selected child and displaying `homework_child_identity_required` with an incorrect instruction to re-ingest. Actual browser testing exposed it; previous browser fixtures granted microphone access and the nonfatal-transport test did not exercise browser permission rejection. Microphone failure now remains a logged warning for homework, preserving on-screen responses and child identity; voice-only sessions retain their existing requirement. New hook/identity regressions reproduce the failure for both math and spelling. No assistance, scoring, or persistence rule changes.

- 2026-09-09 full-menu source-confirmation hang: the menu kept a persistent readline reader active while its ingestion subprocess asked for confirmation. The actual isolated run displayed the prompt and echoed `yes`, but the parent consumed the input and the child waited indefinitely. Prior unit tests injected the confirmation callback, and the real stdin menu fixture never included a nested interactive question. No provider or publication log followed the prompt, correctly locating the stall before confirmation. A pseudo-terminal subprocess regression reproduced `CHILD_INPUT_LOST`; the menu now creates and closes its reader per question, leaving stdin to the active child. The regression proves child receipt and successful return to the menu. No capture, academic contract, or paid work changes.

- 2026-09-09 real-menu spelling intake blocker: the live provider captured all 20 school words but put informational OCR/header/duplicate-page notes into the only available uncertainty field. Intake treated any entry as blocking, and its new spelling CLI early return never offered the existing source-confirmation capability. The user's request for an ordinary ingestion exposed this before Discovery; logs showed the notes and preserved the paid response, but prior AI labs supplied `uncertainty: []` or invoked confirmation directly rather than exercising recovery from the menu. New captures separate `sourceNotes` from page-resolved assigned-word/scope issues. Legacy prose stays pending until explicit default-no confirmation; a separate confirmation record preserves the original receipt. Regressions cover the real menu subprocess, harmless notes, actionable issues, decline/non-interactive behavior, immutable legacy replay, and zero repeated capture calls. Only the spelling intake/recovery boundary changed; the full live board/games/Elli journey is still a separate acceptance gate.

- 2026-09-09 human-caught spelling presentation regression: the targeted spelling worker bypassed the existing node-artwork stage after the Discovery-first early return, while the screenshot fixture incorrectly inherited Discovery's solid background. The human compared the board with Sunny's illustrated maps; successful readiness/evidence logs could not reveal this missing presentation work, and the lab asserted launches but not asset loading or artwork reuse. The worker now restores saved assignment-scoped illustrations and runs the existing best-effort image stage after playable nodes publish, without modifying frozen Planner/academic receipts. Regressions cover no-key reuse, explicit assignment ownership, partial failure, restart, and canonical readiness. The school-sheet browser proof uses production Planner materialization plus labeled saved artwork at both viewports. The worker fixture also freezes its clock: its previously fixed checkpoint date had moved before the live test-created prediction date. No new learning behavior or paid calls are part of this fix.

- 2026-09-08 follow-up readonly review: a non-math label alone did not prove that the chart's current session plan/profile belonged to that assignment after interrupted math publication. Four red regressions cover a math current-plan file, a math profile-plan, stale profile-homework, and a different spelling assignment ID. The publication guard now permits only coherent selected projections; mixed state stays blocked rather than showing the wrong content. This narrows the cross-domain guard fix without changing legacy selection or publication algorithms.

- 2026-09-08 cross-domain release audit: the math publication journal blocked every `getChildChart` read, including an already-committed spelling/reading lane. The human asked for cross-domain assurance before merging; a read-only review identified the overly broad guard and isolated tests reproduced it. Logs could report publication pending but could not distinguish a healthy unrelated lane, and prior crash tests only resumed math. The chart now retains the publication guard for selected math or unknown selection while allowing another selected committed domain. Regression tests leave the journal intact and prove math still fails closed. No family data changed.

- 2026-09-08 child-entry regression: preventing the human-caught blank homework canvas also hid the existing child picker and fatal voice-error recovery. Prior labs preselected a child and a source-string assertion merely required the missing-identity panel. The actual browser now tests no-child startup, child selection, and a recorded fatal voice outage for math, spelling, and reading, with one session attempt and zero learning writes. Picker recovery precedes the homework branch only when there is no resolved child; preselected Discovery retains its direct entry. Test-first evidence is in `output/cross-domain-readiness-20260908/`.

- 2026-09-08 spelling proof integrity: `closedLoopAdaptationProof` claimed runtime adaptation after applying a lab-only target selector to a copied board. Production `retargetFuturePracticeNodes` intentionally keeps the authored session plan unchanged. The human's question about assessing ten words before targeting gaps exposed the coverage mismatch; logs described the lab's synthetic diff and existing assertions accepted it instead of comparing actual runtime output. The lab now observes the actual map result and reports NOT PROVEN when targets do not change. The missing current-session spelling evaluation-to-Planner handoff remains an explicit product gap; this audit does not add deterministic pedagogy or another pipeline.

- 2026-09-08 full frontend audit: two stale source/mock assertions expected no fetch cancellation signal and an always-visible companion, despite the accepted cancellation and end-session behavior. Updated those assertions without changing runtime behavior; cancellation/packet recovery and session-finish regressions remain covered separately.

- 2026-09-07 clean-snapshot audit: two recorded-provider ingestion tests passed locally but failed without `.env` because they stubbed Anthropic credentials while the repair preflight also required OpenAI credentials. This was a test-fixture dependency, not a reason to weaken preflight. The isolated snapshot exposed `preflight_missing:OPENAI_API_KEY`; the prior lab inherited local configuration. Both fixtures now supply dummy OpenAI credentials, with the existing mocked transport and exact call-count assertions unchanged. Red/green evidence: `output/dinner-readiness-20260907/`.

- 2026-09-07 merge-packaging audit: CI installed npm packages but did not provision Chromium for the real-browser tests. Local runs used an already-installed browser, so they could not establish fresh-runner readiness. A failing workflow invariant now proves the missing prerequisite; CI installs Chromium and OS dependencies before running tests. Local regression and build pass; execution on a fresh GitHub runner remains a CI gate, not a claimed local result. Required untracked source/tests must be included deliberately, while all preexisting child-data and generated-artifact deltas remain excluded.

- 2026-09-07 approved repair follow-up: the previously rejected Logbook hidden-keypad defect is resolved in the isolated proof. One explicitly approved GPT-5.6 call used the shared root-cause contract and both exact failure-state screenshots; it added only `#pad-area[hidden]{display:none}`, with every script block unchanged. Every item passed at both release viewports; actual wrong→recovery→completion and server restart preserved canonical first responses and source-child inventories. Five artifacts are now browser-accepted. This closes this generated-artifact defect, not whole-branch release readiness. Historical failures below remain part of the reliability record. Evidence: `output/pipeline-stability-20260907/approved-repair-2/REPORT.md`.

- 2026-09-07 real-host evidence audit: Logbook completion correctly saved an unscored explanation as canonical practice, but legacy fact-bank logging separately labeled that generated explanation correct. Direct feedback also deferred with `direct_feedback_invalid_outcome`. The complete host replay exposed the discrepancy; canonical tests and artifact browser checks independently passed because neither asserted every downstream consumer. Logs revealed it only when compared with the canonical six-observation record. Red-green regressions now keep raw canonical-math attempts out of legacy stores and companion correctness notes, resolve child/domain/item ownership from server context, make ratings engagement-only, and retain valid non-math behavior. The existing canonical completion/Planner path remains the sole academic authority; no extra interpretation call is added. Preview also exits before chart access. Evidence: `output/pipeline-stability-20260907/downstream-authority-green.log`.

- 2026-09-07 full-host replay: the board allowed reopening a completed activity, but its completion endpoint rejected it as `learning_cycle_node_not_launchable`, preventing the rating/return flow. A child would only discover this after replaying every item. Server logs showed the 409, while the earlier lab only reopened the completed activity and returned without completing it; separate first-completion tests passed. Red-green tests now permit replay of completed nodes as practice, preserve current lifecycle and siblings, derive repetition from server state, and suppress duplicate Planner work and completion credit. Quest/Boss evidence gates remain intact. The regression also covers agency and non-agency routes, missing historical item observations, and repeated completion delivery.

- 2026-09-07 completion-display audit: the actual-host screenshot showed 83% from the generated six-item claim while canonical evidence scored 4/5 (80%) and left the explanation unscored. A human comparing the score and report could catch it; the logs only proved the server's score and the old browser test only checked that the modal opened. The existing completion response now returns saved canonical accuracy; the host overlay uses it and omits unknown scores. Both red-green request/display tests and the actual six-item replay assert 80%, not the generated 83%.

- 2026-09-07 independent replay review: reopening a completed instrument while its Planner decision was pending could cause a revision conflict, response redelivery without a stable session ID could append another observation batch, and generic teaching completion also admitted Discovery. A child would see deferred progression or duplicate results; old logs reported only revision conflict, and the lab had neither interleaved replay with a pending decision nor redelivered the actual renderer payload. Red-green tests now preserve one pending decision across append-only practice replay without claiming it cited later evidence, reject other context changes, bind completion to a host-owned launch UUID, and keep Discovery on its dedicated unchanged-on-repeat endpoint. No extra model call or retry was added.

- 2026-09-07 quiet-exit proof: Closing the isolated math map without speaking called the session summarizer with an empty prompt; closing during startup could also leave a late-opening speech connection alive after cleanup. Live exit/reload caught the error and repeated connection logs. Prior browser fixtures mocked voice startup and waited for successful initialization, so they missed the cancellation window. `session-memory-empty.test.ts` proves zero summary/psychologist/context calls for an empty conversation and preserves normal conversation recording. `session-voice-connection.test.ts` proves an ended session cannot start another connection and a pending connection is closed exactly once if it finishes after exit. The repeated live exit logs now show `empty_conversation` and `not_a_learning_session`; a fast close shows speech connection skipped. No learning conclusion is created from an empty conversation.

- Deferred, outside Phase 1's explicit economy scope: the live isolated replay exposed five XP per empty session because `progression.ts` includes `totalSessions * 5`. Logs made the change visible; the map/ingestion lab had not asserted rewards for zero-attempt visits. No economy rule was changed during this work. Add a dedicated zero-participation reward invariant before revising the existing reward policy.

- 2026-09-07 independent repair review: Interruption after saved repair HTML but before the second browser pass left only the first/original iteration hash in the checkpoint. A verifier upgrade rejected that valid intermediate checkpoint; changed prompt bytes could then purchase the same logical repair again. A parent would notice another long repair/spend on restart. Existing logs showed only revalidation/repair, and upgrade fixtures included finished second passes, missing this precise interruption window. Two red-green cases now preserve the paid bytes and consumed allowance across the upgrade, reverify locally, and either accept or stop with zero additional repair calls, including repeated restarts. No generated HTML or provider response was changed.

- 2026-09-07 repair-contract follow-through: A saved activity repaired its first reported control defect but leaked a numeric panel on the final explanation item. The full child-like replay reached the transition; opening screenshots and first-failure repair fixtures did not. Logs previously named `math_hidden_state_visible:#pad-area` without the active item, computed display, or remaining unverified questions. The shared verifier now reports those factual details and both existing Creator repair paths use one root-cause contract, replacing duplicated "only listed defects" instructions. Tests prove the exact request receives that contract and screenshots, and that unvisited items are not labeled passed. This does not establish improved model repair success: no new paid response was requested, and the saved rejected HTML remains untouched and unpublished.

- Deferred after Phase 1 acceptance: automatic engineering-lesson retrieval or promotion across builds. Reuse existing provider/verifier audits, regression tests, and the shared repair contract now. Do not create another AI role, memory store, or self-editing prompt process. Future acceptance would measure recurring defect rates, first-pass acceptance, repair count, cost, and latency on comparable held-out artifacts; failures in Sunny's implementation must never become child preferences or academic evidence.

- 2026-09-07 approved repair proof, remaining content defect: After repairing the graph controls and correcting verifier state synchronization, the saved Logbook activity reaches its sixth explanation item but `#pad-area` remains visible despite `hidden` because of its CSS display rule. Both release viewports reject it; the candidate is not installed. The full journey catches what opening-only checks and a first-defect repair request cannot expose. One authorized repair cost an estimated $0.099208; no further retry or manual HTML edit was performed. Status: open generated-artifact acceptance failure, with screenshots and provider receipt in `output/pipeline-stability-20260907/approved-repair-1/`.

- 2026-09-07 approved repair proof: The aggregate browser verifier demanded interaction semantics from graph points belonging to later questions while they were still noninteractive scenery. The repair correctly added semantics when points become active, but a screen-wide scan of every future journey selector rejected it before the first answer; a subsequent check also ran during the prior item's 650 ms transition. The full saved-artifact replay exposed the false rejection; logs misleadingly named the future item while showing the opening or previous question's screenshot. The earlier lab used controls that were always active and therefore missed this state mismatch. Red-green two-question graph fixtures now accept deferred activation, wait within the existing bounded readiness timeout, and still reject the same point when required without semantics. The aggregate scan is scoped to the current journey item; every selected control, viewport, visibility, obstruction, scoring/event requirement, and repair limit remains enforced. No additional provider call is needed to reverify the saved response.

- 2026-09-07 Phase 1: A repeated Discovery completion after final board publication reset the canonical lifecycle to `evidence_ready` and replaced the original completion time. The restart audit reproduced it with a completed recorded board. Logs treated the replay as a normal completion; the lab had tested duplicate attempts and worker restart but not duplicate completion after publication. A repository-level idempotence guard now returns the unchanged cycle, and a regression asserts identical cycle/summary and zero repeated builders.

- 2026-09-07 Phase 1: The progress watcher treated an empty planning node list as finished, ignored the first status, and silently stopped after ten checks. The user saw a static handoff with no useful explanation; worker logs could be correct while the client stopped watching. Earlier tests began with populated node lists. The watcher now includes pre-map planning/design, exposes its bounded pause and read-only Check progress action, and scopes in-flight results to the assignment. Worker failures, including missing saved extraction, durably enter `needs_attention` rather than leaving a planning screen indefinitely.

- 2026-09-07 Phase 1: Companion bootstrap bypassed the child-invoked wake gate and spoke an opening; the loading curtain also waited for that speech before becoming ready. The full isolated app exposed the contradiction. Opening logs recorded speech but did not assert that a summon occurred, while the browser lab injected fake opening audio. Bootstrap now honors the wake gate; silent math readiness uses boot readiness without requiring first audio. Unit red-green tests and a no-opening-audio browser replay cover both sides.

- 2026-09-07 Phase 1: Independent review reproduced a transient board-packet HTTP failure removing the last valid board/active iframe, a hung status request disabling manual recovery indefinitely, and Finish for now stopping voice without leaving the homework surface. Server logs could show a recoverable network failure or ended voice session but not the stranded screen. The lab previously exercised only successful fetches and did not click Finish. Packet refresh now preserves same-child valid state, status failures remain retryable with request deadlines, and the existing SessionEnd surface is reused for a non-auto-returning homework exit. Tests include failure recovery, timeout, explicit return, and full browser Finish/Return.

- 2026-09-07 Phase 1 map follow-up: The saved graph board crowded its shared-node labels and appended completed Discovery into the new teaching map. The live app exposed what the two-node fixture and artifact-only gates missed; the older layout test used a 2048-pixel spelling board and duplicated markup without lock captions. The repository now retains Discovery in canonical history/nodePlan while excluding it from targeted presentation only after both contracts exist, including stale reloads. Existing code-owned coordinate defaults now separate shared nodes and longer routes without changing Planner topology/counts. Independent review added uneven route, eight-activity, partial-hash, and nonmath-edge regressions. All 171 layout cases (all splits for 2/4/7/8 activities plus the actual five-activity shape at three viewports) pass using the real React renderer/CSS; the saved real board passes both supported viewports. Larger boards are not certified by this bounded matrix. No generated HTML or academic contract was edited.

- 2026-09-07 Phase 1 capability limit: The Visual Explainer registry advertises teaching capability, but available demos cover erosion/blood flow and the older math candidate covers centimeters/inches. No verified graph-compatible bridge with canonical assisted-exposure acceptance was found. It is not being shown as filler or claimed as integrated; new explainer generation was explicitly excluded. Status: compatibility/acceptance gap, not a reason to infer that the child needs an unrelated lesson.

- 2026-09-07: The real returning-board journey briefly displayed “board not ready” and suggested ingestion before the first packet request had finished. A child switch could likewise expose the previous child's packet for a render. The human/autonomous browser journey caught the false screen; successful server responses produced no error log, while prior component tests started after loading. The hook now scopes its snapshot to the requested child and reports loading immediately until that scope has a result. Two render-hook regressions reproduce initial loading and cross-child stale state. Status: focused tests and production build pass; Phase 1 repeated both the recorded browser journey and the actual isolated returning-board launch successfully.

- 2026-09-07: A bounded repair fixed a hidden explanation panel but missed simultaneously visible SVG answer targets without semantics; subsequent clicking also exposed perpetual geometry motion. The complete browser journey caught the second defect after the repair budget was spent. Earlier diagnostics returned at the first page failure, and fixture controls were static and labeled. The verifier now aggregates hidden-panel, clipping/obstruction, semantics, accessible-name, and interaction-stability failures for visible controls at the failure state. It keeps both viewport screenshots and never relaxes the acceptance gate. Status: browser regressions pass; the saved remaining node is still rejected and no additional paid repair is authorized.

- 2026-09-07: Restart review found targeted design checkpoints were saved but not loaded, initial builder responses could be lost between HTML and metadata installation, and the Anthropic SDK could retry a request before the durable receipt layer regained control. This is a spending/recovery defect exposed by interruption tests, not a child-content defect. Happy-path fixtures missed disk-install interruption and SDK-internal retries. Existing provider receipts now cover targeted design and initial build before parsing, design snapshots are loaded and saved atomically, and the durable SDK request disables retries. A fake-fetch SDK regression went from three requests to one; interrupted builder installation now reuses one completed response. Status: focused tests/build and narrow readonly review pass.

- 2026-09-07: Upgrading the visual verifier could discard a prior repair's consumed allowance and permit another call. Repeated-restart fixtures, including skipped verifier versions, exposed this; version logs alone did not prove spending bounds. Migration preserves paid repaired bytes and a durable repairConsumed marker while discarding only obsolete verdicts. Tests prove zero additional repair calls across repeated restarts and skipped versions. Status: fixed and independently reviewed; exhausted work is not reset for the live proof.

- 2026-09-07: In the isolated returning-board session, Elli's opening referred to an Independent Discovery challenge even though Discovery was complete and the targeted map was visible. The complete host journey caught the context mismatch; artifact-only verification cannot inspect companion context, and existing logs do not establish that her opening matches the visible lifecycle. Status: Phase 1 fixed the unsolicited opening by honoring the existing child-invoked gate and verified silent launch in the actual isolated app. A summoned returning-board conversation still needs separate live acceptance; no canned replacement line was added.

- 2026-09-07: The shared verifier waited for `evaluation_attempt` even when verifying a teaching node whose approved prompt and host use `attempt_event`. The full autonomous journey rejected three saved nodes as missing responses; a real browser click emitted the correct teaching event at both release sizes. Logs recorded a false instrument failure because the verifier listened to the wrong protocol. Worker tests mocked browser success, and the synthetic teaching fixture omitted per-attempt messages, so the lab missed the disagreement. The verifier now selects the actual protocol by activity lifecycle; browser tests cover teaching navigation, missing commits, incorrect protocol, and Discovery preservation. Verifier version 4 invalidates the stale verdict, and a worker test proves local re-verification precedes paid repair.

- 2026-09-07: Targeted artifact generation and final publication built child paths from the source checkout while canonical cycle IO honored SUNNY_CONTEXT_ROOT. The supposedly isolated run therefore saved five HTML artifacts in the source Ila tree. Source inventory also differs in seven existing files; attribution of those other mutations is unresolved and no historical child data was restored speculatively. The human's complete impersonator journey exposed the boundary; logs named files but did not assert that every output shared the selected context root. Tests had used rootDir and contextRoot pointing to the same place. Publication regression now deliberately separates them and asserts zero source-child writes. All three direct-artifact path sites use the existing context resolver.

- 2026-09-07: The unfinished targeted repair called the provider again on replay, silently dropped missing screenshots, and checked frozen metadata after overwriting HTML. The autonomous review caught the write order and reproduced all five defects with fake providers, including interrupted installation and malformed output. Happy-path mocked repair and Discovery-only receipt tests missed this. Targeted repair now uses the existing durable provider receipt mechanism, keeps original HTML and exact request, validates metadata/screenshots before spending, and atomically installs the repair. Tests also prohibit real fetch in the worker fixture. No new AI role or content-generation path was introduced.

- 2026-09-07: The fresh Day 22 graph build used a perpetual scale animation on every answer tile. The first browser pass reported only missing interaction semantics, so the one bounded repair added roles and labels but left the tiles continuously moving; Playwright then could not obtain a stable click and the second pass failed. The autonomous full certification caught it before publication. Logs accurately exposed the second-pass click timeout but the first-pass diagnostic did not report the coexisting animation defect, and prior fixtures used static controls. The control preflight now reports perpetual geometry animation as `interaction_stability` alongside all other defects, allowing one repair to address the complete failure set without prescribing visual style. Status: fixed in focused test; fresh isolated certification pending.

- 2026-09-07: A fresh isolated Day 22 graph certification produced an item whose representation and accepted answer used a five-unit graph interval, while its evidence construct was named `construct:scale-count-by-tens`. The autonomous child-journey review caught this by inspecting the frozen academic contract before allowing design/build to continue. Existing logs recorded only the contract hash, and browser/scoring gates could prove that the generated UI matched the same internally inconsistent contract but could not prove that its evidence label described the mathematics shown. Earlier fixtures used aligned labels and representations, so the AI lab never challenged this boundary. The pipeline now rejects explicit numeric-interval contradictions before saving or reusing a Planner checkpoint, and the Planner must self-check evidence labels against linked representations and answers. Status: fixed in focused test; full isolated certification pending.

- 2026-09-06: The production-shaped math-ingestion acceptance test mocked Anthropic but allowed the new default GPT-5.6 repair path to use the real global `fetch`. Changing the repair prompt invalidated the old receipt hash, so a supposedly zero-cost gate contacted OpenAI and exposed the account's current `no credits remaining` response. The test log showed the provider receipt but the test did not enforce network isolation. Fixed by stubbing the GPT Responses stream with a surgical patch fixture and asserting exactly one repair request plus zero repeats after publication. The complete recorded ingestion now repairs 51 source characters, verifies both viewports, publishes, and resumes without any external provider. Status: fixed; the live saved-clock proof remains blocked by provider credits.

- 2026-09-06: Impersonator certification reused an isolated workspace copied before the repair-provider change because its identity included only the assignment and child snapshot, not the executable pipeline. The autonomous full-journey run caught the stale `claude-sonnet-5` repair path before another provider request; prior logs truthfully said `Reusing isolated workspace` but never proved which code was inside it, and tests varied child/assignment state without varying implementation bytes. Certification identity and validation now include a hash of copied executable source while excluding family context and runtime logs. Tests prove unchanged code resumes and changed code creates a new workspace. Status: fixed; no provider spend occurred during discovery.

- 2026-09-06: The first full isolated ingestion after the model comparison showed why repair success cannot compensate for an unreliable initial build. Sonnet exhausted the 48k output ceiling and returned truncated HTML. A GPT-5.6 replacement build completed, but its first pass contained obstructed SVG targets; the one bounded GPT repair fixed those and introduced a non-actionable final 7:45 hour-hand drag target at 1280x720. Browser logs named the exact item and selector and publication remained blocked; the AI comparison had exercised a different frozen activity, so it did not establish cross-assignment first-pass reliability. Status: unresolved release blocker; no manual HTML edit, second repair, publication, or canonical child write is permitted.

- 2026-09-06: The saved clock Discovery failed because `#hand-long` was a visible SVG hit target with no interaction semantics or accessible name, but the browser gate reduced this to a generic missing-control failure and the repair model received opening screenshots instead of the exact failed journey state. The human caught the unusable clock during the real impersonator journey; logs rejected publication but omitted the item, selector, geometry, obstruction, and missing behavior, while prior AI labs checked opening layouts and mocked repair boundaries rather than traversing the generated SVG control. The verifier now aggregates both release viewports, captures the screen at failure, and reports exact item/control diagnostics to repair without weakening usability gates. A single measured Sonnet repair fixed the SVG semantics but added multiple response actions to auto-advancing Discovery items; the strengthened verifier now rejects this immediately as `math_journey_steps_after_commit` instead of timing out on a vanished selector. Status: pipeline diagnostics fixed; the one-call repair proof failed safely, no artifact was published, Ila/Reina remained unchanged, and any model comparison is a separate experiment.

- 2026-09-06: The real Sunny menu and its `sunny:certify` subprocess both owned readers for the same terminal. During an impersonator launch, the parent menu could buffer or consume the dragged PDF response before certification read it; certification then normalized an empty value to the worktree directory and reported `certification_assignment_missing:<worktree>`. The human caught it on the exact menu journey. The log exposed the wrong resolved path but not the competing terminal readers, while menu tests mocked the subprocess and certification tests invoked it independently, so the AI lab never exercised the nested stdin handoff. Fixed by making the menu the sole assignment-input owner: it normalizes and validates the file, passes an explicit `--pdf`, and certification is noninteractive whenever that argument is present. A setup-only certification gate permits the exact menu-to-subprocess path to be exercised without provider calls. Status: fixed; exact zero-cost terminal replay required before human retry.

- 2026-09-06: The first real impersonator ingestion failed preflight with `unknown companion preset "elli"` because workspace isolation rewrote `children.config.json` with only the selected `childProfiles` entry and accidentally removed shared companion presets while retaining the selected child's companion mapping. The human caught it by exercising the first real certification run; logs exposed the missing preset but mislabeled the wrapper defect as assignment input, while fixture tests modeled only child profiles and omitted the shared configuration dependency. Fixed by preserving shared companion definitions and defaults, filtering only child-specific profile/mapping collections, validating companion/profile integrity before ingestion, and versioning certification manifests so the broken workspace cannot resume. Status: fixed; no provider call occurred and human replay is pending.

- 2026-09-06: Impersonator certification printed nothing after accepting the assignment path while it validated and copied the isolated workspace, making a safe setup phase look frozen; Finder-style dragged paths containing escaped spaces were also not normalized. The human caught the silence immediately because the terminal was the only visible surface. No phase log existed yet, and unit tests called workspace creation directly with normalized fixture paths, so neither logs nor the lab exercised the real paste-and-wait experience. Fixed with four truthful phase indicators (validate, isolate, prepare Discovery, launch journey) and dragged-path normalization. Percent completion remains deliberately absent for provider work because its duration is not knowable. Status: fixed; human replay pending.

- 2026-09-06: The session curtain displayed `100%` after its safety timeout even while voice readiness remained false, and a homework launch without a resolved child identity could fall through to the generic companion canvas. The human caught both by launching from the familiar `sunny` command and watching the full transition; the visible status itself contradicted 100%, while the white companion page was obviously not a homework state. Logs recorded a safety release as if it were readiness and the component test explicitly accepted that behavior; browser labs began with a resolved child and board, so they never exercised the missing-identity launch boundary. Fixed by keeping progress factual during safety release and making every homework runtime remain inside an explicit homework surface, including a named missing-identity failure. Regression tests cover both invariants. The observed screenshots were additionally launched from the stale `feature/baseline-game-factory` checkout, so the acceptance command must name and verify the implementation worktree before launch. Status: fixed in `codex/adaptive-math-discovery-board`; human replay pending.

- 2026-09-06: Parent preview was assumed to validate the complete adaptive math journey, but preview correctly blocked evidence persistence, so Discovery could never trigger targeted planning or board generation. The human caught the mismatch by asking how a parent could inspect the final map, Elli/VRM, and ceremonies; logs showed a successful read-only preview but could not reveal the missing downstream experience because no write or generation was expected. The AI lab separately tested stateless preview and persistent child progression, so it missed the product requirement to exercise both through one safe developer workflow. The new invariant replaces math parent preview in the menu with an impersonator certification journey that persists only in a full isolated workspace, scopes workers/server/browser state to one run, disables log upload, and verifies the source child inventory remains unchanged. Focused isolation/menu/server tests, production-shaped ingestion/browser/worker tests, both source and copied-workspace builds, and source fingerprint verification pass. Status: fixed; paid human journey remains the acceptance step.

- 2026-09-06: A successful isolated Day 22 ingestion printed `Start session: npm run sunny`, but the sandbox `SUNNY_CONTEXT_ROOT` had been scoped to the completed ingestion command. Following the printed instruction launched the menu against the default canonical context, where no matching math cycle existed, and failed with `No math homework cycle found for ila`. The human caught this because they followed the real two-command parent journey; logs correctly reported the missing cycle but omitted that ingestion and launch had resolved different context roots. The acceptance lab kept both operations inside one process environment and therefore missed shell-boundary context loss. Fixed by printing a copyable session command that preserves the exact isolated context and protected-child opt-in. The regression test reproduces separate-command handoff semantics. Status: fixed.

- 2026-09-05: The ingestion boundary audit reproduced extraction envelope/raw mismatch, complete-browser verification outside repair, weak published-artifact reuse checks, and response/receipt/publication/ownership recovery gaps. The real session exposed boundaries that helper-only and opening-screen tests did not exercise. These are now covered by the actual intake entry point with mocked SDK + real Chromium + isolated persistence, explicit malformed/uncertain receipt tests, publication interruption and evidence fences, and worker-compatible extraction fixtures. A null Skip transport failure also became a host regression while preserving uncaptured-answer uncertainty. Details, retained limitations, and release gate results: `docs/MATH_INGESTION_TEST_READINESS.md`. The same audit found Discovery omitted the original PDF that targeted planning already attached; both now share the attachment path with frozen-byte hash checks, relocated-cache path correction, and an empty-evidence refusal before spending. No paid provider call or historical child-evidence repair was performed.

- 2026-09-05: The paid Day 22 ingestion exposed two context/source-resume defects before Discovery publication. The parent menu listed children from the repository even when `SUNNY_CONTEXT_ROOT` pointed at an isolated acceptance copy, so Ila could be selected without an isolated profile; the resulting local setup failure was mislabeled `PROVIDER_PAUSED`. After that was fixed, the human-readable archive log exposed that stale-source archival moved the newly extracted assignment into `audit/` without restoring it to the active draft, which would later strand the targeted worker. The human caught the first problem because the menu offered an impossible choice and the second because the archive message appeared during a real resume. Logs named the archive but did not prove the active extraction still existed; prior labs started from fresh temporary drafts and never combined a stale source checkpoint with a current extraction. New menu, failure-classification, ordering, and source-archive invariants cover both boundaries. Status: fixed; paid Planner/design checkpoints preserved in the isolated Ila draft for reuse.
- 2026-09-05: Adaptive-math release regressions are tracked in `docs/ADAPTIVE_MATH_RELEASE_REPORT.md`. Confirmed failures: the worker published job readiness before canonical artifact binding/prediction registration; generated reports fabricated browser success; prediction matching pooled pre-registration, assisted, and unrelated source batches; Discovery friction was unconsumed and engagement delivery could block academic completion. The human caught trapped screens and invalid graph questions because they experienced the whole journey. Logs described generation/scoring events without proving visible playability or prediction timing; the old lab tested helpers and generated completion hooks instead of real host controls and persistence. New worker, evidence, coordinator, and real-browser invariants cover these boundaries. Synthetic answer-leaking and inconsistent-scale graph cases are preserved; parent assessment-validity review remains pending.
- 2026-09-05: Independent release review reproduced late-sibling publication resetting academic lifecycle, final publication deleting newly prescribed Support, stale revisions losing validated Support publication, inferred legacy eligibility, and premature on-load completion passing decorative controls. These interleavings and proof failures were absent from earlier helper tests and opening-only logs. Focused red-green regressions now preserve canonical interventions, immutable records, explicit eligibility, and control-linked browser evidence. Review recheck passed; release remains gated on final test results and parent acceptance.

- 2026-08-31: Read-only merge review found the new Discovery visual-review renderer started its temporary HTTP server before Playwright import/browser launch, but registered cleanup only after launch succeeded. A Playwright import or launch failure could therefore leave the server listening. The human originally caught clipped generated controls because ingestion logs proved generation rather than rendered usability; the first visual lab added screenshot review but did not simulate launch failure, so neither logs nor the AI lab observed resource cleanup. Fixed by putting import, launch, page work, browser close, and server close under one shared lifecycle boundary. A launch-failure invariant now proves no temporary server remains listening. Status: fixed.
- 2026-08-31: Read-only merge review found Discovery academic binding proved only that item IDs, construct IDs, and accepted-answer strings occurred somewhere in generated HTML. A repaired artifact could retain those strings in comments or unrelated data while changing scoring behavior. The human caught visible layout failures, but logs and the AI visual lab inspected presentation rather than exercising every frozen answer contract, so semantic drift could pass silently. Fixed by requiring an exact structured runtime contract and a side-effect-free bridge to the activity's real scorer. Before publication, Playwright now proves every frozen accepted answer is accepted with the correct item/construct identity and a deterministic non-answer is rejected. Status: fixed.
- 2026-08-06: The complete merge gate passed but dirtied tracked Ila/Reina and QA context files because several legacy tests still use the repository context root instead of temporary fixtures. Assertions and production behavior were green, so ordinary test output did not identify the contamination; the post-test Git audit caught it. The generated changes were quarantined and excluded from the merge. Expected follow-up: migrate remaining writers to isolated context roots and add a CI invariant that canonical child files remain byte-identical after `npm test`. Status: open; does not block the code-only math merge because contaminated files are excluded.
- 2026-08-04: A complete parent-preview playthrough found Lightning Door reached its final explanation prompt while still showing the prior comparison buttons, making the route impossible to complete honestly. Opening smoke passed because the first screen was sound; logs had no browser exception until the sixth item; prior checks never traversed response-mode changes. Root cause in the generated implementation was calling the coin renderer for an explanation item with no coin array before replacing the prior controls. Fixed at the production boundary by requiring mode-matched controls and a same-handler full playthrough through the final item, and by running an available generated playthrough inside the existing bounded browser diagnostic. The first isolated rebuild then passed the complete interaction journey but clipped its keypad at 1365×768; a second rebuild passed that raw viewport but the real 1280×720 embedded frame still clipped the final row. The ingestion smoke had tested a larger viewport than the host, so it reported a false pass. The generic builder contract and browser diagnostic now both cover Sunny's 1280×720 embedded minimum and require measured enabled-control bounds. N3B was rebuilt separately from its frozen academic/design contracts and completed all six embedded rounds without clipped enabled controls. Status: fixed.
- 2026-08-04: The stricter 1280×720 smoke check reported Puzzle Door horizontal overflow even though its complete interaction area and every control were visible. Browser inspection showed the generated activity intentionally keeps a fixed 1365×768 decorative canvas while both `html` and `body` declare `overflow:hidden`; there was no child-visible scrollbar or clipped control. The logs caught raw document width but could not distinguish hidden decorative overflow from navigable overflow. The diagnostic now reports horizontal overflow only when the document is wider and both root overflow policies permit it to be visible; a zero-cost resumed ingestion passed the corrected browser diagnostic. Status: fixed.
- 2026-08-04: A complete embedded Lightning Door playthrough reached the rating modal, which displayed the internal engine label `Generated Baseline` instead of the AI-authored activity title. Runtime logs correctly proved completion but did not inspect child-facing overlay copy; earlier tests covered overlay mechanics without a generated node title. The shared title resolver now prefers `NodeConfig.title` and retains the type-derived label only as a legacy fallback. A second complete embedded journey displayed `The Lightning Door: Beam Surge Rounds`, accepted a four-star rating, and returned to the map. Status: fixed.
- 2026-08-04: The complete web suite caught a delayed session-start timer updating React after the companion-audio test environment had unmounted (`window is not defined`). Focused tests passed because they ended before the ten-second connection timeout; the full suite's longer workload exposed the orphaned poll and timeout. Fixed by owning both timers in the session hook, replacing prior startup timers on a new start, and clearing both during unmount. Status: fixed.
- 2026-08-04: Browser review found the parent-preview badge remained fixed above an open generated activity and could cover its top controls. The human release path caught the same class of host/activity composition defect that generated-HTML checks cannot see; the non-blocking browser smoke check verified the iframe's controls existed but did not include the host preview overlay in its geometry assertions. Fixed by retaining preview inspection and write blocking while showing the badge only on the map, where it cannot obscure activity controls. Status: fixed.
- 2026-08-04: The complete release suite showed `session-manager.ts` at 2,607 lines, beyond its existing 2,500-line maintainability gate. The growth came from companion presence, read-aloud memory, and transcript routing being implemented directly in the central session class. Human companion testing motivated those behaviors, but focused behavior tests did not run the repository-wide size invariant. Fixed by moving pure companion-presence, activity-help context/memory, transcript acceptance, and speech-formatting logic into the existing focused support modules; the session manager now delegates and is back under the unchanged budget. Status: fixed.
- 2026-08-04: Full release tests found `parseMathLearningProgram` still rejected a complete Planner program when an activity carried a redundant route alias or a custom shared-foundation route identity. The board was supposed to project the Planner's two declared route arms, but parser duplication treated any third activity route string as a fatal creative-shape error. Human ingestion failures exposed the general “whiteboard rejected the painter” class; narrow tests had separately covered only literal `shared` and exact route IDs. Fixed by reconciling aliases from the Planner-authored fork membership and preserving activities outside both arms as shared foundations without rewriting their identity. Status: fixed.
- 2026-08-04: The direct math board mounted the legacy `questBossArtifactPreparation` hook even though canonical node completion now owns evidence-gated Quest/Boss decisions and generation. A normal board open could therefore start a paid legacy preparation from artifact presence rather than qualifying child evidence, while its status poll could not observe the canonical generator. The human did not see an obvious error because both paths logged as Quest/Boss preparation; prior tests mocked each path independently instead of proving the direct board had one authority. Fixed by removing legacy preparation and polling from `useChildExperiencePacket`; the direct board now loads canonical state only, remains stable for the finished session, and receives the generated next chapter on the next session load. Legacy map callers remain unchanged. Status: fixed.
- 2026-08-04: Real/interactive session shutdown logged `learning_cycle_compatibility_projection_drift:hw-math-c26ba5e8:r2` while `finalizeSession` attempted to persist the profile. The human saw the shutdown failure because session logs exercised the presentation-rich compatibility plan; canonical repository tests had only asserted a presentation-free projection, so neither the logs nor prior lab covered this boundary. Fixed by making projection IDs idempotent, validating approved presentation projections against immutable canonical fields, and normalizing compatibility plans through the canonical cycle before profile persistence. The lab invariant now exercises authored presentation plans directly. Status: fixed.
- 2026-08-04: Human visual review caught generic, stale, and low-contrast math node images even though ingestion logs reported dedicated thumbnails as generated/reused. The logs verified asset creation but not the final browser URL; prior tests verified stored bindings but missed two later presentation rewrites: the canonical lifecycle projection could replace baseline artwork, and the child-experience projection always replaced route artwork with opening screenshots. Stable filenames also allowed browser caching after an asset replacement. Fixed with content-versioned thumbnail filenames, presentation-owned baseline artwork, and opening screenshots used only as a fallback. Lab invariants now assert both the canonical merge and the final child-experience projection preserve dedicated thumbnails.
- 2026-08-03: Zero-cost coin-ingestion acceptance reused all paid checkpoints and reached FULL, but the non-blocking browser smoke check reported `N1:title_not_visible` and `N3A:browser_error:Cannot read properties of null (reading 'classList')`. The human release audit required playable nodes; ingestion logs correctly exposed both failures, while the prior lab did not block grandfathered v13 artifacts. Fixed with a targeted N1/N3A rebuild from frozen academic/design contracts. N2/N3B, Planner, Fable design, artwork, and board were reused; the four-node opening smoke check now passes. Status: fixed.
- 2026-08-06: Merge-readiness verification exposed duplicate `edge-quest-to-boss` identities in the current Planner board. React warned that the duplicated key could omit or duplicate an edge, but prior assertions checked visible nodes rather than projection identity. Fixed by deduplicating presentation edges at the board projection boundary and adding an invariant against duplicate edge IDs. Status: fixed.
- 2026-08-03: Math-ingestion ROI leak: optional bonus node N4 was built eagerly even though it was absent from the published board and only belongs to the earned reward/video-call flow. The coin ingestion spent about 313 seconds and 32,046 recorded tokens on this invisible artifact. Fixed by preserving the Planner/Fable bonus contract while deferring its HTML build until earned; baseline ingestion reports `Bonus: deferred until earned`, excludes it from baseline publication requirements, and has a focused regression test. Status: fixed.
- 2026-08-03: Grok can ignore the dedicated-thumbnail prohibition against text and invent tiny pseudo-labels or symbols. The board now uses separate circular-safe thumbnail prompts instead of activity screenshots, which fixes crop and contrast, but prompt wording alone cannot guarantee clean image typography. Expected follow-up: compare a provider that reliably follows no-text composition or add a bounded asset-validation/replacement step without turning visual judgment into child-learning evidence. Status: visible provider limitation; do not block the math lifecycle work.
- 2026-08-03: Reward follow-up after the math release gate: inventory should remain freely available, while the store is revealed as an earned destination with a strong world reaction. Experiment separately with a server-owned variable reward ratio (earned pulls tied to verified challenge completion, bounded rewards, no mastery effect) and a Mario-style growth/reveal moment. Do not add this before the Slice Sprint sandbox and current child-journey acceptance are complete.
- 2026-08-03: Human route review required the chosen path to become visibly available while its sibling becomes unavailable. The existing test used the serialized pre-choice board and therefore could not exercise the transition; logs only proved the callback, and the prior lab checked sibling locking without checking the selected destination. Fixed the fixture to enter through an available choice gate and fixed local projection to expose the selected node while labeling the sibling `Route not picked`. Server lifecycle remains authoritative. Status: fixed.

- 2026-07-18: Full root Vitest had three fixture/catalog failures: two tests requested the registered-but-unseeded `demo-pashley` profile, and the generated-shell test depended on mutable Reina catalog state. Fixed by using existing chart fixtures for planning comparisons and seeding the generated-shell catalog contract directly inside its test. Production no longer needs to fabricate a profile or preserve stale child runtime data just to keep the suite green. Status: fixed 2026-08-04.
- 2026-07-31: Elli presentation polish is deferred until the math learning loop is complete. Reduce the full-board VRM scale, keep Elli clear of nodes and controls, add a consistent high-contrast portrait frame inside activities, and use a subtle speaking-state highlight. Preserve the rule of one Elli representation per screen. Status: deferred; do not interrupt math acceptance, Quest/Boss progression, or returned-work calibration.

- 2026-06-01: Full root Vitest has one unrelated companion video-call trace regression in `src/server/game-event-handler.test.ts`: `companion_tic_tac_toe_child_move` is recorded as `{ type, game: "unknown" }` instead of the expected payload with `activityId`, `companionId`, and `square`. Expected: companion video-call activity events should preserve the structured payload in the shared session game trace stream. Status: discovered while verifying Sunny ingest main port; deferred to keep the ingest merge scoped.

- 2026-05-16: Product complaints inside a spelling session can still remain only in transcript if they do not match the current complaint detector. Example: Ila saying Spell Check does not say the word before spelling should become a bounded `product_issue` chart record automatically. Status: deferred from Spelling Session Trust Patch to keep the patch scoped.

- 2026-05-16: Reward/coin companion copy is not fully grounded across all games. Wheel-style games can expose exact game coin values while the companion may still paraphrase or exaggerate totals if context is stale or incomplete. Expected: companion should only speak coin totals from the latest canonical game state or currency award. Status: deferred from Spelling Session Trust Patch to keep the patch scoped.

- 2026-05-12: `src/tests/test-map-coordinator.ts` onboarding preview test expects `onboarding-academic-load-check`, but the current onboarding map only returns `onboarding-bubble-pop` and `onboarding-dopamine-break`. This is outside the adaptive homework evidence/quest scope.

- 2026-05-12: Pronunciation regression: after a perfect 10/10 run, `PronunciationGameCanvas` ends as soon as `isComplete` flips, so the child loses the celebration/result moment too quickly. Expected: keep the completed screen/game state visible long enough for feedback and companion acknowledgement before returning to the map. Status 2026-05-13: fixed with completion overlay, replay, and harder replay.

- 2026-05-12: Companion-care treats feel ignored during assistant turns. `game-event-handler.ts` logs `skip spoken reply — companion already owns the turn` and returns when turn state is not `IDLE`; it records the event/animation but does not queue a later care acknowledgement. Expected: never interrupt an active turn, but queue one short post-turn acknowledgement or visible-only acknowledgement state. Status: open.

- 2026-05-12: Homework map still feels scripted after strong evidence. Word selection and node order remain deterministic (`selectHomeworkSessionWords` + `HOMEWORK_NODE_ORDER`), so a second spelling run can reuse the same words and same baseline sequence despite 100% performance. Expected: an organic session composer should rotate word order, change evaluator mode/difficulty, and vary equivalent valid nodes based on prior session evidence. Status 2026-05-13: partially addressed by chart-driven active session plans, mystery choice, adaptive cohorts, and organic map variation; needs one more live session before marking fixed.

- 2026-05-13: Companion can answer child/player meta-debug comments too literally during a live game. Example: Reina asked if there were bugs, and Matilda answered as if inspecting the screen instead of recording a possible complaint and staying anchored to the active activity state. Expected: companion should route bug/complaint-like utterances into bounded child signals or parent-visible notes, then respond briefly without pretending certainty. Status 2026-05-13: fixed for active-game transcripts by urgent learning support routing; bug/lag language records `recordProductIssue` instead of pretending certainty.

- 2026-05-13: Ila homework preflight shows `pendingHomework` is still `hw-reading-bfd2d4a8` with test date 2026-05-06 and no `activeSessionPlan`. Expected: before a real Ila homework run, ingestion/planning should refresh the chart or runtime should clearly present a fallback plan instead of silently using stale homework context. Status 2026-05-13: resolved for the current Ila spelling cycle by ingestion and approved active session plan; needs regression coverage before marking broadly fixed.


## Paid math ingestion evidence gate — 2026-09-05

- Reproduced: correct practice/repeated responses qualified for Quest; generated baseline `correct` flags became canonical truth without checking frozen answers. Missing answers and same-completion retries could manufacture independent evidence. Human review caught the meaning of the question/response across the journey; previous logs repeated generated success, and the browser lab exercised controls without adversarial response claims. New `mathEvidenceRelease.test.ts` invariants cover wrong claims, uncaptured responses, exposure, target alignment, duplicate rows/options and missing progression contracts.
- Fix: Quest requires captured, correct, unseen independent target-aligned evidence. Canonical math observations score selection IDs, numeric text and captured construction state against immutable Planner items. Explanations, invalid capture and missing legacy contracts stay unscored. Support/Quest/Boss freeze the same contracts before generation. Retry events remain separate from the first response.
- Delete: the second direct-item parser was removed and its stricter validation reused in the existing parser. No new production module, AI role or preview system. Historical family evidence is not rewritten.
# Discovery rating Skip reopens the completed evaluation (2026-09-07)

- **Human-visible failure:** After all seven Discovery items completed and the child selected `Skip fun rating`, the first Discovery item reopened instead of showing `Sunny is preparing your learning path`.
- **Why the human/autonomous journey caught it:** The failure exists only across the real sequence `evaluation_complete → engagement overlay → Skip → packet refresh`; isolated component tests stopped before observing the post-refresh surface.
- **Why logs did not catch it:** Server logs truthfully recorded seven observations, `evidence_ready`, and one worker launch. The regression was client-local state being cleared by a refreshed cycle revision, so no server error existed.
- **Why the AI lab missed it:** The lab asserted that the handoff resolver returned `targeted-planning`, but did not assert that a packet revision preserves that handoff and prevents Discovery auto-relaunch.
- **Required invariant:** A learning-cycle revision within the same child and homework assignment must preserve the completion handoff; only changing child or homework may reset board-session state.

# Targeted Planner failure loses the paid response (2026-09-07)

- **Human-visible failure:** Discovery completed successfully, but background generation stopped with `math_learning_program_requires_activities`, leaving no response artifact that explained whether the Planner emitted no activities or only bonus activities.
- **Why the human/autonomous journey caught it:** Only a complete live journey invokes targeted planning from freshly committed Discovery evidence; fixture tests preload a valid `math-learning-program.json`.
- **Why logs did not catch it:** The worker log kept only the parser's downstream error and discarded the provider response, model usage, and stop reason.
- **Why the AI lab missed it:** The lab verified that malformed plans stop without an AI repair loop, but did not verify that the malformed paid response is durably preserved or that a program contains route-bearing baseline activities.
- **Required invariant:** Every targeted Planner call must save its raw response before parsing, and a learning program must contain at least two baseline activities plus its optional bonus contract.
- 2026-09-09 evidence-first release review: frozen spelling `attempt_event` messages could still enter the legacy word-bank scorer. A human could see a correct/incorrect game result diverge from the canonical report; legacy logs would misleadingly say the attempt was recorded. The initial browser lab forwarded state and narration but omitted attempt events. It now forwards the real SessionManager handler, asserts no legacy scorer calls, and exercises canonical routes at both viewports. Ownership tests also reject mislabeled-domain bypasses while preserving unrelated legacy games.

- 2026-09-09 evidence-first release review: completing a recall-check replay reused the original canonical answers. Humans can see the new response immediately; prior tests covered duplicate initial completion, not a genuinely new replay. New tests require the actual replay value, repeated-practice provenance, no independent mastery, stable prediction evaluations, and idempotency. The fix changes the initial-capture condition, not the scoring rules.

- 2026-09-09 repair-evidence review: a later failed publication revalidation left an older successful engineering lesson eligible. Existing logs described the failure, but the eligibility reader still trusted the audit. Earlier tests manually revoked lessons instead of exercising the actual publication path. A red-green production-path test now verifies revocation for future requests and preservation of historical snapshots for completed requests.

- 2026-09-09 native spelling release proof: Letter Rush silently used sample content after a frozen configuration failed to load. A human would recognize the wrong words; old demo-oriented tests deliberately accepted defaults. The new bound mode fails visibly, disables Start, emits only an instrument issue, and preserves host exit. Eighteen isolated browser cases cover both frames and valid frozen-ID capture; legacy unbound demos are unchanged.
- 2026-09-09 full-App replay regression: the backend correctly treated completed checkpoints as practice, but the open React launch retained its old assessment flag. A human using Play again would immediately encounter rejected submissions. Logs recorded the rejected attempts; the earlier backend-only replay proof never clicked the actual button. The browser proof now completes a checkpoint, clicks Play again, records new practice outcomes, and asserts no assessment POSTs, no rewritten observations, and no reevaluated predictions at both frame sizes. Replay refreshes the assignment-bound node and resets its capture coordinator; leaving/switching scope invalidates pending replay.

- 2026-09-09 assignment-bound checkpoint/catalog review: fixed node IDs such as practice can recur across assignments. Earlier tests resumed the same selected assignment, masking shared Creator checkpoint directories and catalog reads from the selected rather than requested cycle. Tests now use two assignments with identical node IDs and switch selected domains during worker resume. Creator inputs/receipts must remain assignment-scoped, matching legacy completed receipts remain reusable, and catalog provenance must follow the worker's explicit assignment. These are storage/identity invariants, not content-specific patches.

- 2026-09-10 fresh spelling targeted-Planner schema failure: the live school-word journey committed all 20 Discovery items, then the background worker stopped because future Quest and Boss measurements had empty `evidenceIds`. The human requested screenshots of the complete journey, which forced the test past the opening activity and exposed the stopped Preparing state. Logs did preserve the exact schema paths and raw paid response, but the child-facing preparation screen did not surface the failure; recorded Planner fixtures always supplied valid citations, so the AI lab never exercised a model response that was academically complete but structurally invalid. The pipeline now gives the same Planner one bounded, checkpointed schema-correction turn with the exact canonical ID allowlist, preserves both receipts and combined usage, rejects a second invalid correction, and saves the derived program checkpoint only after semantic validation. The saved-response live resume succeeded: six playable nodes became Ready, Quest/Boss remained evidence-locked, and the lifecycle reached `board_ready` without repeating Discovery. Status: fixed and covered by the fresh school-sheet proof.

- 2026-09-10 fresh spelling microphone-fallback regression: the first targeted Word Radar activity logged that microphone access was unavailable and promised on-screen controls, but its resolved activity mode still hid the keyboard. The human-visible journey caught the contradiction because the child could see the warning and no way to answer; the log captured microphone failure but did not assert that an advertised fallback was actually rendered, and prior labs tested the warning and keyboard-capable component separately. The hook now carries actual microphone availability into Word Radar's resolved mode, so a voice-configured activity exposes the on-screen keyboard when voice is unavailable. Fifty-nine component and websocket regressions cover the bound fallback. Current-code screenshots show the child entering `sign` and receiving `Got it!`.

- 2026-09-10 isolated certification write leak: the fresh impersonator journey correctly isolated canonical learning-cycle data but legacy session notes, narrative context, and debug logs still defaulted to the source checkout. Git status and file timestamps exposed writes to real Reina paths after the run; the logs explicitly reported successful writes but did not include their resolved physical roots, and prior labs fingerprinted canonical evidence without exercising every legacy writer. Red tests reproduced all three leaks. Legacy context writers now resolve through the selected context root, debug logs default beneath an explicit context root, and certification supplies its own session-log root. The exact leaked test note/context block and generated logs were removed. Thirty isolation/bootstrap regressions pass, and the fresh proof remains physically contained.

- 2026-09-10 stale post-Discovery greeting: after the targeted spelling board reached `board_ready`, session bootstrap still chose the completed Discovery node for its greeting and said the independent challenge was ready. The full return journey exposed the stale message; lifecycle logs were correct, but the greeting helper ignored completed-node history, and earlier tests used only an unfinished opening node. Bootstrap now filters completed nodes before selecting the greeting subject. The canonical completed Discovery remains in history and evidence; only the spoken return context advances to the first unfinished targeted node.

- 2026-09-10 merge-gate isolation regression: the full map-coordinator test suite wrote six synthetic activity completions into Reina's real learning profile, session notes, decision traces, and engagement theory. The certification source fingerprint caught the mutation after the fresh spelling proof; individual test assertions and ordinary logs did not fail because the fixtures used a real child ID and the writers correctly recorded to the unresolved default root. The AI lab missed the boundary because its test setup isolated generated map files but not every child-context writer. Map-coordinator tests now set a temporary `SUNNY_CONTEXT_ROOT`, seed their own profiles, and assert the session-note path resolves inside that root. The exact synthetic records were removed and Reina's directory was restored byte-for-byte to the certification snapshot before rerunning the merge gate.

- 2026-09-10 learning-engine test isolation regression: read-only release review found `learningEngine.test.ts` still resolving Ila to the repository context and restoring only a subset of the compatibility projections it could write. A human would see stale or inflated child state later, while each test stayed green because it restored the primary profile and word bank; ordinary logs named successful writes but did not identify that their physical root was canonical. Earlier isolation work covered certification and map coordination, not this legacy suite. The test now creates and seeds a temporary `SUNNY_CONTEXT_ROOT`, asserts it never resolves to the family directory, and deletes that root after the suite.

- 2026-09-10 spelling homophone eligibility regression: CMU pronunciation evidence correctly protected `sea/see`, but also treated informal spellings and text shortcuts such as `thru`, `kool`, `u`, and `r` as instrument ambiguity. A child or parent would recognize these as spelling misses; the logs would truthfully preserve the response but misclassify the academic fact, and the initial lab covered only standard homophones plus `nite`. The authoritative scorer now excludes audited nonstandard CMU forms while retaining standard same-pronunciation words, with regression cases for phonetic, advertising, and single-letter shortcuts.

- 2026-09-10 mystery-finalization test isolation regression: the legacy mystery reward test ran `planSession`, `recordAttempt`, and `finalizeSession` against canonical Ila data while restoring only the profile and word bank. Finalization also writes care-plan, homework, catalog, decision-trace, engagement, and session-note projections, so a green test could silently alter the child chart. The full merge inventory caught the extra files; ordinary assertions and logs treated those writes as expected behavior, and the focused test never checked the resolved context root. The suite now seeds a temporary child chart, asserts that its root is not canonical, and removes the complete isolated root afterward.

- 2026-09-10 map handoff test isolation regression: the voice-handoff tests mocked legacy per-word attempts but still executed real activity-model, adaptive-load, currency, trace, and compatibility-projection writes for Ila. Repeated green runs inflated activity counts and even created math-domain evidence for spelling activities. The family inventory and trace contents exposed the leak; prior tests verified handoff payloads but not storage location. The whole test file now runs against a freshly seeded temporary `SUNNY_CONTEXT_ROOT`, and a release invariant rejects the canonical family root.

- 2026-09-10 canvas voice-sync test isolation regression: a simulated Word Builder round exercised the real `SessionManager` persistence path as Ila, adding an `add` SM-2 result and reward currency while every voice-timing assertion passed. The byte-level family fingerprint caught it after all 3,536 behavioral tests were green; logs showed a normal game completion and gave no indication that a test had resolved the canonical chart. The voice-sync suite now seeds a fresh temporary child chart before every case, asserts its resolved root is non-canonical, and removes it after the file completes.

- 2026-09-10 legacy clock/progression test isolation regression: `clockTracker.test.ts` and `progression.test.ts` used the literal child ID `ila`, edited the repository profile/word bank, and restored only those two files. `writeLearningProfile` also rewrote the homework, active-plan, care-plan, and catalog waterfall, so the full suite passed while replacing Ila's active chart with whichever cycle was hydrated during the test. The family fingerprint caught the mutation; ordinary logs reported valid writes without their resolved root, and prior unit tests checked numerical outcomes rather than physical isolation. Both suites now create complete temporary context roots, use dedicated lab child identities, assert that paths cannot resolve to `src/context/ila`, and remove the whole sandbox after each case.

- 2026-09-10 math-interleaving test isolation regression: the legacy Elli math-tool integration test called the real learning engine as `Ila`, recording `4+1` and `7+2` into the canonical word bank while restoring only a previously existing bank. When another leaking test had already emptied that bank, the suite left synthetic math evidence behind despite all 3,537 root assertions passing. The full family fingerprint caught the final mutation; ordinary tool logs omitted the resolved storage path, and the test asserted recommendations rather than storage isolation. The suite now runs the same Ila-facing tool against a fresh temporary context root, asserts the word-bank path is not canonical, and removes the sandbox after every case.
