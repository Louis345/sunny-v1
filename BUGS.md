# Bugs

- 2026-08-31: Read-only merge review found the new Discovery visual-review renderer started its temporary HTTP server before Playwright import/browser launch, but registered cleanup only after launch succeeded. A Playwright import or launch failure could therefore leave the server listening. The human originally caught clipped generated controls because ingestion logs proved generation rather than rendered usability; the first visual lab added screenshot review but did not simulate launch failure, so neither logs nor the AI lab observed resource cleanup. Fixed by putting import, launch, page work, browser close, and server close under one shared lifecycle boundary. A launch-failure invariant now proves no temporary server remains listening. Status: fixed.
- 2026-08-31: Read-only merge review found Discovery academic binding proved only that item IDs, construct IDs, and accepted-answer strings occurred somewhere in generated HTML. A repaired artifact could retain those strings in comments or unrelated data while changing scoring behavior. The human caught visible layout failures, but logs and the AI visual lab inspected presentation rather than exercising every frozen answer contract, so semantic drift could pass silently. Fixed by requiring an exact structured runtime contract and a side-effect-free bridge to the activity's real scorer. Before publication, Playwright now proves every frozen accepted answer is accepted with the correct item/construct identity and a deterministic non-answer is rejected. Status: fixed.
- 2026-08-06: The complete merge gate passed but dirtied tracked Ila/Reina and QA context files because several legacy tests still use the repository context root instead of temporary fixtures. Assertions and production behavior were green, so ordinary test output did not identify the contamination; the post-test Git audit caught it. The generated changes were quarantined and excluded from the merge. Expected follow-up: migrate remaining writers to isolated context roots and add a CI invariant that canonical child files remain byte-identical after `npm test`. Status: open; does not block the code-only math merge because contaminated files are excluded.
- 2026-08-04: A complete parent-preview playthrough found Lightning Door reached its final explanation prompt while still showing the prior comparison buttons, making the route impossible to complete honestly. Opening smoke passed because the first screen was sound; logs had no browser exception until the sixth item; prior checks never traversed response-mode changes. Root cause in the generated implementation was calling the coin renderer for an explanation item with no coin array before replacing the prior controls. Fixed at the production boundary by requiring mode-matched controls and a same-handler full playthrough through the final item, and by running an available generated playthrough inside the existing bounded browser diagnostic. The first isolated rebuild then passed the complete interaction journey but clipped its keypad at 1365×768; a second rebuild passed that raw viewport but the real 1280×720 embedded frame still clipped the final row. The ingestion smoke had tested a larger viewport than the host, so it reported a false pass. The generic builder contract and browser diagnostic now both cover Sunny's 1280×720 embedded minimum and require measured enabled-control bounds. N3B was rebuilt separately from its frozen academic/design contracts and completed all six embedded rounds without clipped enabled controls. Status: fixed.
- 2026-08-04: The stricter 1280×720 smoke check reported Puzzle Door horizontal overflow even though its complete interaction area and every control were visible. Browser inspection showed the generated activity intentionally keeps a fixed 1365×768 decorative canvas while both `html` and `body` declare `overflow:hidden`; there was no child-visible scrollbar or clipped control. The logs caught raw document width but could not distinguish hidden decorative overflow from navigable overflow. The diagnostic now reports horizontal overflow only when the document is wider and both root overflow policies permit it to be visible; a zero-cost resumed ingestion passed the corrected browser diagnostic. Status: fixed.
- 2026-08-04: A complete embedded Lightning Door playthrough reached the rating modal, which displayed the internal engine label `Generated Baseline` instead of the AI-authored activity title. Runtime logs correctly proved completion but did not inspect child-facing overlay copy; earlier tests covered overlay mechanics without a generated node title. The shared title resolver now prefers `NodeConfig.title` and retains the type-derived label only as a legacy fallback. A second complete embedded journey displayed `The Lightning Door: Beam Surge Rounds`, accepted a four-star rating, and returned to the map. Status: fixed.
- 2026-08-04: The complete web suite caught a delayed session-start timer updating React after the companion-audio test environment had unmounted (`window is not defined`). Focused tests passed because they ended before the ten-second connection timeout; the full suite's longer workload exposed the orphaned poll and timeout. Fixed by owning both timers in the session hook, replacing prior startup timers on a new start, and clearing both during unmount. Status: fixed.
- 2026-08-04: Browser review found the parent-preview badge remained fixed above an open generated activity and could cover its top controls. The human release path caught the same class of host/activity composition defect that generated-HTML checks cannot see; the non-blocking browser smoke check verified the iframe's controls existed but did not include the host preview overlay in its geometry assertions. Fixed by retaining preview inspection and write blocking while showing the badge only on the map, where it cannot obscure activity controls. Status: fixed.
- 2026-08-04: The complete release suite showed `session-manager.ts` at 2,607 lines, beyond its existing 2,500-line maintainability gate. The growth came from companion presence, read-aloud memory, and transcript routing being implemented directly in the central session class. Human companion testing motivated those behaviors, but focused behavior tests did not run the repository-wide size invariant. Fixed by moving pure companion-presence, activity-help context/memory, transcript acceptance, and speech-formatting logic into the existing focused support modules; the session manager now delegates and is back under the unchanged budget. Status: fixed.
- 2026-08-04: Full release tests found `parseMathLearningProgram` still rejected a complete Planner program when an activity carried a redundant route alias or a custom shared-foundation route identity. The board was supposed to project the Planner's two declared route arms, but parser duplication treated any third activity route string as a fatal creative-shape error. Human ingestion failures exposed the general “whiteboard rejected the painter” class; narrow tests had separately covered only literal `shared` and exact route IDs. Fixed by reconciling aliases from the Planner-authored fork membership and preserving activities outside both arms as shared foundations without rewriting their identity. Status: fixed.
- 2026-08-04: The direct math board mounted the legacy `questBossArtifactPreparation` hook even though canonical node completion now owns evidence-gated Quest/Boss decisions and generation. A normal board open could therefore start a paid legacy preparation from artifact presence rather than qualifying child evidence, while its status poll could not observe the canonical generator. The human did not see an obvious error because both paths logged as Quest/Boss preparation; prior tests mocked each path independently instead of proving the direct board had one authority. Fixed by removing legacy preparation and polling from `useChildExperiencePacket`; the direct board now loads canonical state only, remains stable for the finished session, and receives the generated next chapter on the next session load. Legacy map callers remain unchanged. Status: fixed.
- 2026-08-04: Real/interactive session shutdown logged `learning_cycle_compatibility_projection_drift:hw-math-c26ba5e8:r2` while `finalizeSession` attempted to persist the profile. The human saw the shutdown failure because session logs exercised the presentation-rich compatibility plan; canonical repository tests had only asserted a presentation-free projection, so neither the logs nor prior lab covered this boundary. Fixed by making projection IDs idempotent, validating approved presentation projections against immutable canonical fields, and normalizing compatibility plans through the canonical cycle before profile persistence. The lab invariant now exercises authored presentation plans directly. Status: fixed.
- 2026-08-04: Human visual review caught generic, stale, and low-contrast math node images even though ingestion logs reported dedicated thumbnails as generated/reused. The logs verified asset creation but not the final browser URL; prior tests verified stored bindings but missed two later presentation rewrites: the canonical lifecycle projection could replace baseline artwork, and the child-experience projection always replaced route artwork with opening screenshots. Stable filenames also allowed browser caching after an asset replacement. Fixed with content-versioned thumbnail filenames, presentation-owned baseline artwork, and opening screenshots used only as a fallback. Lab invariants now assert both the canonical merge and the final child-experience projection preserve dedicated thumbnails.
- 2026-08-03: Zero-cost coin-ingestion acceptance reused all paid checkpoints and reached FULL, but the non-blocking browser smoke check reported `N1:title_not_visible` and `N3A:browser_error:Cannot read properties of null (reading 'classList')`. The human release audit required playable nodes; ingestion logs correctly exposed both failures, while the prior lab did not block grandfathered v13 artifacts. Fixed with a targeted N1/N3A rebuild from frozen academic/design contracts. N2/N3B, Planner, Fable design, artwork, and board were reused; the four-node opening smoke check now passes. Status: fixed.
- 2026-08-06: Merge-readiness verification exposed duplicate `edge-quest-to-boss` identities in the current Planner board. React warned that the duplicated key could omit or duplicate an edge, but prior assertions checked visible nodes rather than projection identity. Fixed by deduplicating presentation edges at the board projection boundary and adding an invariant against duplicate edge IDs. Status: fixed.
- 2026-08-03: Math-ingestion ROI leak: optional bonus node N4 was built eagerly even though it was absent from the published board and only belongs to the earned reward/video-call flow. The coin ingestion spent about 313 seconds and 32,046 recorded tokens on this invisible artifact. Fixed by preserving the Planner/Fable bonus contract while deferring its HTML build until earned; baseline ingestion reports `Bonus: deferred until earned`, excludes it from baseline publication requirements, and has a focused regression test. Status: fixed.
- 2026-08-03: Grok can ignore the dedicated-thumbnail prohibition against text and invent tiny pseudo-labels or symbols. The board now uses separate circular-safe thumbnail prompts instead of activity screenshots, which fixes crop and contrast, but prompt wording alone cannot guarantee clean image typography. Expected follow-up: compare a provider that reliably follows no-text composition or add a bounded asset-validation/replacement step without turning visual judgment into child-learning evidence. Status: visible provider limitation; do not block the math lifecycle work.
- 2026-08-03: Reward follow-up after the math release gate: inventory should remain freely available, while the store is revealed as an earned destination with a strong world reaction. Experiment separately with a server-owned variable reward ratio (earned pulls tied to verified challenge completion, bounded rewards, no mastery effect) and a Mario-style growth/reveal moment. Do not add this before the Slice Sprint sandbox and current child-journey acceptance are complete.
- 2026-08-03: Human route review required the chosen path to become visibly available while its sibling becomes unavailable. The existing test used the serialized pre-choice board and therefore could not exercise the transition; logs only proved the callback, and the prior lab checked sibling locking without checking the selected destination. Fixed the fixture to enter through an available choice gate and fixed local projection to expose the selected node while labeling the sibling `Route not picked`. Server lifecycle remains authoritative. Status: fixed.

- 2026-07-18: Full root Vitest had three fixture/catalog failures: two tests requested the registered-but-unseeded `demo-pashley` profile, and the generated-shell test depended on mutable Reina catalog state. Fixed by using existing chart fixtures for planning comparisons and seeding the generated-shell catalog contract directly inside its test. Production no longer needs to fabricate a profile or preserve stale child runtime data just to keep the suite green. Status: fixed 2026-08-04.
- 2026-07-31: Elli presentation polish is deferred until the math learning loop is complete. Reduce the full-board VRM scale, keep Elli clear of nodes and controls, add a consistent high-contrast portrait frame inside activities, and use a subtle speaking-state highlight. Preserve the rule of one Elli representation per screen. Status: deferred; do not interrupt math acceptance, Quest/Boss progression, or returned-work calibration.

- 2026-06-01: Full root Vitest has one unrelated companion video-call trace regression in `src/server/game-event-handler.test.ts`: `companion_tic_tac_toe_child_move` is recorded as `{ type, game: "unknown" }` instead of the expected payload with `activityId`, `companionId`, and `square`. Expected: companion video-call activity events should preserve the structured payload in the shared session game trace stream. Status: discovered while verifying Sunny ingest main port; deferred to keep the ingest merge scoped.

- 2026-05-16: Product complaints inside a spelling session can still remain only in transcript if they do not match the current complaint detector. Example: Ila saying Spell Check does not say the word before spelling should become a bounded `product_issue` chart record automatically. Status: deferred from Spelling Session Trust Patch to keep the patch scoped.

- 2026-05-16: Reward/coin companion copy is not fully grounded across all games. Wheel-style games can expose exact game coin values while the companion may still paraphrase or exaggerate totals if context is stale or incomplete. Expected: companion should only speak coin totals from the latest canonical game state or currency award. Status: deferred from Spelling Session Trust Patch to keep the patch scoped.

- 2026-05-12: `src/tests/test-map-coordinator.ts` onboarding preview test expects `onboarding-academic-load-check`, but the current onboarding map only returns `onboarding-bubble-pop` and `onboarding-dopamine-break`. This is outside the adaptive homework evidence/quest scope.

- 2026-05-12: Pronunciation regression: after a perfect 10/10 run, `PronunciationGameCanvas` ends as soon as `isComplete` flips, so the child loses the celebration/result moment too quickly. Expected: keep the completed screen/game state visible long enough for feedback and companion acknowledgement before returning to the map. Status 2026-05-13: fixed with completion overlay, replay, and harder replay.

- 2026-05-12: Companion-care treats feel ignored during assistant turns. `game-event-handler.ts` logs `skip spoken reply — companion already owns the turn` and returns when turn state is not `IDLE`; it records the event/animation but does not queue a later care acknowledgement. Expected: never interrupt an active turn, but queue one short post-turn acknowledgement or visible-only acknowledgement state. Status: open.

- 2026-05-12: Homework map still feels scripted after strong evidence. Word selection and node order remain deterministic (`selectHomeworkSessionWords` + `HOMEWORK_NODE_ORDER`), so a second spelling run can reuse the same words and same baseline sequence despite 100% performance. Expected: an organic session composer should rotate word order, change evaluator mode/difficulty, and vary equivalent valid nodes based on prior session evidence. Status 2026-05-13: partially addressed by chart-driven active session plans, mystery choice, adaptive cohorts, and organic map variation; needs one more live session before marking fixed.

- 2026-05-13: Companion can answer child/player meta-debug comments too literally during a live game. Example: Reina asked if there were bugs, and Matilda answered as if inspecting the screen instead of recording a possible complaint and staying anchored to the active activity state. Expected: companion should route bug/complaint-like utterances into bounded child signals or parent-visible notes, then respond briefly without pretending certainty. Status 2026-05-13: fixed for active-game transcripts by urgent learning support routing; bug/lag language records `recordProductIssue` instead of pretending certainty.

- 2026-05-13: Ila homework preflight shows `pendingHomework` is still `hw-reading-bfd2d4a8` with test date 2026-05-06 and no `activeSessionPlan`. Expected: before a real Ila homework run, ingestion/planning should refresh the chart or runtime should clearly present a fallback plan instead of silently using stale homework context. Status 2026-05-13: resolved for the current Ila spelling cycle by ingestion and approved active session plan; needs regression coverage before marking broadly fixed.
